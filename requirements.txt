# ============================================================
# SMAART Career Intelligence Platform — Dependencies
# ============================================================
# This file consolidates all dependencies for the platform.
# For quick setup, use: setup_windows.bat
# ============================================================

# ----------------------------------------------------------
# Python / ML Service  (ml-service/)
# ----------------------------------------------------------
# Install via:  cd ml-service && pip install -r requirements.txt
#
# flask==3.0.3
# flask-cors==4.0.1
# sentence-transformers==3.0.1
# torch==2.3.1
# scikit-learn==1.5.0
# xgboost==2.0.3
# pandas==2.2.2
# numpy==1.26.4

# ----------------------------------------------------------
# Node.js / Backend  (backend/)
# ----------------------------------------------------------
# Install via:  cd backend && npm install
#
# express, prisma, bcryptjs, jsonwebtoken, helmet,
# mongoose, ioredis, cors, dotenv, groq-sdk,
# node-cron, express-rate-limit

# ----------------------------------------------------------
# Node.js / Frontend  (frontend/)
# ----------------------------------------------------------
# Install via:  cd frontend && npm install
#
# react, react-dom, react-router-dom, recharts,
# framer-motion, lucide-react, axios, vite
